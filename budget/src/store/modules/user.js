const user = {
    state: {
        hiddenValue: true,
    },

    mutations: {
        SET_HIDDENVALUE: (state, hiddenValue) => {
            state.hiddenValue = hiddenValue
        },
    },

    actions: {

    }
}

export default user