const user = {
    state: {
        uuid: '',
    },

    mutations: {
        SET_UUID: (state, uuid) => {
            state.uuid = uuid
        },
    },

    actions: {

    }
}

export default user