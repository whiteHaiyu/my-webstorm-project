const user = {
    state: {
        login: false,
    },

    mutations: {
        SET_LOGIN: (state, login) => {
            state.login = login
        },
    },

    actions: {

    }
}

export default user