const user = {
    state: {
        book_id: '',
    },

    mutations: {
        SET_BOOKID: (state, book_id) => {
            state.book_id = book_id
        },
    },

    actions: {

    }
}

export default user